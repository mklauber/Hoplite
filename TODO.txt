
Highlight actor needing input
Team colors (Implement via team names being hex RGB codes?)
Combine Animations
Move all calls to getch into LevelScreen.__call__

Animate Die

Bash onto Lava:
    lava reaction triggers Burn action (Subset of Die Action):
    Burn action replaces burnt element with lava.


Shoot Ability does not handle obstacles while calculating targets.
